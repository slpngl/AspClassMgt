@model IEnumerable<AspClassMgt.Models.Matricula>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InstituicaoIDMatricula)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AlunoIDMatricula)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CursoIDMatricula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InstituicaoIDMatricula)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AlunoIDMatricula)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CursoIDMatricula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdMatricula }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdMatricula }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdMatricula })
        </td>
    </tr>
}

</table>
